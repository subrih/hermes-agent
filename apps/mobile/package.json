{
  "name": "@hermes/mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Kaveri / Hermes mobile client (Capacitor wrapper over the desktop React renderer).",
  "scripts": {
    "build:web": "npm --prefix ../desktop run build",
    "sync": "cap sync ios",
    "open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.4.0",
    "@capacitor/ios": "^8.4.0",
    "@capacitor/preferences": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.0"
  }
}
